import 'package:flutter/material.dart';

class CafeArchives extends StatelessWidget {
  const CafeArchives({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('訪問履歴一覧'),
    );
  }
}
